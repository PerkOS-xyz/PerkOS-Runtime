{
  "name": "perkos-runtime",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "npm start -w @perkos/runtime-desktop",
    "dev": "npm run dev -w @perkos/runtime-web",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^2.1.9"
  }
}
